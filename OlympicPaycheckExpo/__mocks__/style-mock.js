/**
 * Stub for CSS imports.
 *
 * `constants/theme.ts` imports `global.css` for the react-native-web build.
 * Jest has no CSS loader and native never sees the file, so it resolves to an
 * empty module here.
 */
module.exports = {};
